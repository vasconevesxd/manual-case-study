/*import icons from "../../assets/icons/sprite.svg?sprite";

const Icon = ({ name, color, size }) => {
    return (
      <svg viewBox={icons.viewBox}>
        <use xlinkHref={`#${icons.id}`} />
      </svg>
    )
}

export default Icon
*/